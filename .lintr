linters: linters_with_defaults(
    line_length_linter(120),
    object_length_linter = NULL,  # Disable if causing issues
    commented_code_linter = NULL,
    cyclocomp_linter = NULL
  )
exclusions: list(
    "renv",
    "packrat"
  )
